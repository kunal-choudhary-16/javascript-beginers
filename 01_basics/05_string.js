const name = "Kunal"
const repoCount = 55

// console.log(name + repoCount + " Value"); //but this method is too old

console.log(` Hello my name is ${name} and my repo count is ${repoCount}`); //we can use this backtick form for showing string output

//another method for string through object

const gamename = new String('kunalhc') 

// different methods of string  learn all 

console.log(gamename[0]);  //for accessing the element of object 

console.log(gamename.length);
console.log(gamename.toUpperCase());
